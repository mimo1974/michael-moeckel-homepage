# Umami Cloud instead of Google Analytics

The site is hosted on a German (`.de`) domain, and Google Analytics would require a GDPR cookie-consent banner gating it before load. We chose Umami Cloud instead — it doesn't use cookies or personal data by default, so no consent banner is needed, keeping the site simpler and avoiding a compliance surface.
